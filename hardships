1.   the first hardship was the summary which was being generated by the llm, was not matching with 
the schema which i created, due to which it was always throwing error , so i debugged and came to a conclusion 
that we should use different llm for different purposes, as which llm should be  great  for particular work, so 
yes this from this problem


Don't use dictionary syntax on a Beanie document

This is still wrong:

session["conversation"].append(...)

Do
session["conversation"].append(...)

 ///////

 The biggest hardship i struggled was like was shifting the entire workflow from only services to services+langgraph as here i figured out what things to keep in services(like making initial state, DB calls, and then calling langgrah), and what things should be in langgraph(llm callls, updating state, deciding which graph to call next)

///
 reduced response time by 30%, by optimizing prompts for llm,and using differnt llm which are great in doing that particular work


/////

production interview systems never let the LLM control the interview flow completely.

The LLM should recommend a follow-up, but your backend decides whether one is actually allowed.

we check in router that if follow_up <1      - then will return introduction_followup else will generate first question


///
In LangGraph, decision_router is the single source of truth for "what happens next" — every node just does its job and returns state.


///  15 August

in generate_next_question_node we don't have to pass entire conversation as passing the entire objects wastes tokens, so we can optimise it by passing only the question and anser instead of all

question
answer
feedback
improvement
score

